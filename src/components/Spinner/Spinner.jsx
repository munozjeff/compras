import React from "react";
import "./Spinner.style.css"; // Asegúrate de tener este archivo CSS para el estilo

const Spinner = () => {
  return (
    <div className="spinner"></div>
  );
};

export default Spinner;
